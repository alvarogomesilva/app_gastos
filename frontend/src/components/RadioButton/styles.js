import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    buttonReceive: {
        alignItems: 'center',
        justifyContent: 'center',
        borderWidth: 1,
        width: 150,
        height: 50,
        borderRadius: 10,
        flexDirection: 'row',
        gap: 10
    },

    textGreen: {
        color: '#4caf50'
    },

    textRed: {
        color: '#e57373'
    }
})