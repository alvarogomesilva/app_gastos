import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        alignItems: 'center'
    },

    content: {
        width: '90%',
        marginTop: 30,
    },

    title: {
        fontSize: 25,
        textAlign: 'center',
        marginBottom: 10
    }
})