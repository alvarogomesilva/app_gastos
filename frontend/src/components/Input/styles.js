import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    box: {
        marginVertical: 20,
        position: 'relative'
    },

    icon: {
        fontSize: 25,
        position: 'absolute',
        top: 12,
        left: 15,
        color: '#AAA'
    },

    input: {
        borderColor: '#AAA',
        borderWidth: 1,
        height: 50,
        borderRadius: 10,
        paddingLeft: 60,
        fontSize: 20
    },
})